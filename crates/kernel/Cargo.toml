[package]
name = "pythia-kernel"
version = "0.1.0"
edition.workspace = true

[dependencies]
pythia-eventlog = { path = "../eventlog" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
