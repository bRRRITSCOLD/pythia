//! `pythia-cli`: command parsing, composition root (wires concrete `OllamaProvider` + SQLite
//! path + manifest/policy paths into the kernel). Stub binary — logic lands in a later task.

fn main() {
    println!("pythia: workspace scaffold — no commands implemented yet");
}
